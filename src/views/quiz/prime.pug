extends ../layout.pug

block pagetitle
    title n^2 + n + 41

block title
    div(class='container')
        h1 n<sup>2</sup> + n + 41
        p(class='subtitle text-center') Somebody has a conjecture: for every integer n > 1 the number n <sup>2</sup> + n + 41 is prime (is not a product of two smaller integers). Is it true? If not, find a counterexample.

block container
    div(class='center text-center padded m50 prime')
        div(class='row')
            div(class='col-md-6 col-md-offset-3')
                div(class='text-right text') 1 <
                input(class='form-control numberField', placeholder='Type n number here')
                div(class='text-left text') < 1000000
        
    p(class='center text-center')
        button(type='button', class='impossible btn btn-info') Always prime!
        button(type='button', class='check btn btn-info') Check
        button(type='button', class='reset btn btn-info') Reset

    div(class='modal fade', id='prime_modal', tabindex='-1')
        div(class='modal-dialog')
            div(class='modal-content')
                div(class='modal-body') 
                    h4 Error!
                div(class='modal-footer')
                    button(type='button', class='btn btn-default', data-dismiss='modal') Close

block append scripts
    script(src='/javascripts/quiz/prime.js')